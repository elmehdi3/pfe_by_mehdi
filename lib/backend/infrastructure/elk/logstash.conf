input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  if [logger_name] =~ "audit" {
    mutate { add_tag => ["audit"] }
  }
}

output {
  elasticsearch {
    hosts => ["elasticsearch:9200"]
    index => "matchmaker-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
