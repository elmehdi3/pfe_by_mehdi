apiVersion: v1
kind: Service
metadata:
  name: {{ include "matchmaker-backend.fullname" . }}
  labels:
    {{- include "matchmaker-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "matchmaker-backend.selectorLabels" . | nindent 4 }}
