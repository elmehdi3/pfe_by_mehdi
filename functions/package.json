{
  "name": "stitch-teamup-backend",
  "description": "Firebase backend for Stitch TeamUp matchmaking and social features.",
  "scripts": {
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "firebase-admin": "^11.11.0",
    "firebase-functions": "^4.4.0"
  },
  "engines": {
    "node": "20"
  }
}
